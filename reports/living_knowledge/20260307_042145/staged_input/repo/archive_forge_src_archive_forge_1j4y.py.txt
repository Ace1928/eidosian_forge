SOURCE_PATH: archive_forge/src/archive_forge/1j4y.py
KIND: code

class ConfigManager:
    def load_config(self, config_path):
        """Loads configuration settings from a JSON file."""
        with open(config_path, "r") as file:
            return json.load(file)
