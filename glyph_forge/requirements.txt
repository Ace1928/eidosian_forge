# Glyph Forge Requirements (base)
# --------------------------------
# For full feature sets, prefer: pip install -e ".[streaming,browser,tui,audio,assets,clipboard]"

# Core runtime
pillow>=9.0.0
numpy>=1.26.0,<2.0
rich>=13.0.0
typer>=0.9.0
pyfiglet>=0.8.0
PyYAML>=6.0

# Optional extras (install via pyproject extras)
# streaming: opencv-python<4.13, yt-dlp, mss
# browser: playwright
# tui: textual
# audio: pygame, simpleaudio
# assets: requests
# clipboard: pyperclip
