# TODO: eidos-brain

## 🚨 High Priority
- [ ] **Install**: Verify `pip install .[dev]` works.
- [ ] **Audit**: Check `core/` for deprecated Python 3.10 patterns.
- [ ] **Docs**: Merge `knowledge/README.md` insights into main `README.md` or organize `knowledge/` better.

## 🟡 Medium Priority
- [ ] **Refactor**: Move `labs/tutorial_app.py` to a proper `examples/` directory if it's stable.
- [ ] **API**: Add OpenAPI schema validation in `api/`.

## 🟢 Low Priority
- [ ] **UI**: Investigate `ui/` directory status.