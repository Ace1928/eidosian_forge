# TODO: game_forge

## 🚨 High Priority
- [ ] **Standardize**: Add `__main__.py` to each sub-project so they are runnable as modules.
- [ ] **Dependencies**: Verify `pygame` installs correctly in headless environments (if needed).

## 🟡 Medium Priority
- [ ] **Chess**: Connect `agentic_chess` to `agent_forge` for AI players.

## 🟢 Low Priority
- [ ] **Web**: Port `gene_particles` to WebAssembly/PyScript for browser viewing.