# Spatial Hashing Notes

- Spatial hashing maps coordinates to a fixed-size hash table to
  accelerate neighbor queries.
- Uniform grids can be implemented with hashed keys or dense arrays.
- Hashing is especially useful for sparse, unbounded, or dynamic domains.
