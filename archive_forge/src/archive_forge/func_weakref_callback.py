from __future__ import annotations
import itertools
import typing
import warnings
import weakref
def weakref_callback(weakref):
    o = obj_weak()
    if o:
        self.disconnect_by_key(o, name, key)