import numpy as np
import sys
from PyQt5.QtCore import Qt
from PyQt5 import QtWidgets
import pyaudio
def apply_distortion(self, sound: np.ndarray, drive: float, tone: float) -> np.ndarray:
    """Distorts the sound based on drive and tone settings."""
    pass