import datetime
import warnings
import weakref
import unittest
from test.support import gc_collect
from itertools import product
def test_with(self):
    with self.assertRaises(ValueError):
        self.foo()