from __future__ import absolute_import, division, print_function
import re
from ansible_collections.ansible.netcommon.plugins.module_utils.network.common.rm_base.network_template import (
class Prefix_listsTemplate(NetworkTemplate):

    def __init__(self, lines=None, module=None):
        prefix = {'set': 'set', 'remove': 'delete'}
        super(Prefix_listsTemplate, self).__init__(lines=lines, tmplt=self, module=module, prefix=prefix)
    PARSERS = [{'name': 'name', 'getval': re.compile('\n                ^set\n                \\spolicy\n                \\sprefix-(?P<afi>\\S+)\n                \\s(?P<name>\\S+)\n                $', re.VERBOSE), 'setval': "policy prefix-{{ 'list' if afi == 'ipv4' else 'list6' }} {{ name }}", 'result': {"{{ 'ipv4' if afi == 'list' else 'ipv6' }}": {'afi': "{{ 'ipv4' if afi == 'list' else 'ipv6' }}", 'prefix_lists': {'{{ name }}': {'name': '{{ name }}'}}}}}, {'name': 'description', 'getval': re.compile("\n                ^set\n                \\spolicy\n                \\sprefix-(?P<afi>\\S+)\n                \\s(?P<name>\\S+)\n                \\sdescription\\s'(?P<description>.+)'\n                $", re.VERBOSE), 'setval': "policy prefix-{{ 'list' if afi == 'ipv4' else 'list6' }} {{ name }} description '{{ description }}'", 'result': {"{{ 'ipv4' if afi == 'list' else 'ipv6' }}": {'afi': "{{ 'ipv4' if afi == 'list' else 'ipv6' }}", 'prefix_lists': {'{{ name }}': {'name': '{{ name }}', 'description': '{{ description }}'}}}}}, {'name': 'sequence', 'getval': re.compile('\n                ^set\n                \\spolicy\n                \\sprefix-(?P<afi>\\S+)\n                \\s(?P<name>\\S+)\n                \\srule\\s(?P<sequence>\\d+)\n                $', re.VERBOSE), 'setval': "policy prefix-{{ 'list' if afi == 'ipv4' else 'list6' }} {{ name }} rule {{ sequence }}", 'result': {"{{ 'ipv4' if afi == 'list' else 'ipv6' }}": {'afi': "{{ 'ipv4' if afi == 'list' else 'ipv6' }}", 'prefix_lists': {'{{ name }}': {'name': '{{ name }}', 'entries': {'{{ sequence }}': {'sequence': '{{ sequence }}'}}}}}}}, {'name': 'action', 'getval': re.compile("\n                ^set\n                \\spolicy\n                \\sprefix-(?P<afi>\\S+)\n                \\s(?P<name>\\S+)\n                \\srule\\s(?P<sequence>\\d+)\n                \\saction\\s'(?P<action>permit|deny)'\n                $", re.VERBOSE), 'setval': "policy prefix-{{ 'list' if afi == 'ipv4' else 'list6' }} {{ name }} rule {{ sequence }} action '{{ action }}'", 'result': {"{{ 'ipv4' if afi == 'list' else 'ipv6' }}": {'afi': "{{ 'ipv4' if afi == 'list' else 'ipv6' }}", 'prefix_lists': {'{{ name }}': {'name': '{{ name }}', 'entries': {'{{ sequence }}': {'sequence': '{{ sequence }}', 'action': '{{ action }}'}}}}}}}, {'name': 'rule_description', 'getval': re.compile("\n                ^set\n                \\spolicy\n                \\sprefix-(?P<afi>\\S+)\n                \\s(?P<name>\\S+)\n                \\srule\\s(?P<sequence>\\d+)\n                \\sdescription\\s'(?P<rule_description>.+)'\n                $", re.VERBOSE), 'compval': 'description', 'setval': "policy prefix-{{ 'list' if afi == 'ipv4' else 'list6' }} {{ name }} rule {{ sequence }} description '{{ description }}'", 'result': {"{{ 'ipv4' if afi == 'list' else 'ipv6' }}": {'afi': "{{ 'ipv4' if afi == 'list' else 'ipv6' }}", 'prefix_lists': {'{{ name }}': {'name': '{{ name }}', 'entries': {'{{ sequence }}': {'sequence': '{{ sequence }}', 'description': '{{ rule_description }}'}}}}}}}, {'name': 'ge', 'getval': re.compile("\n                ^set\n                \\spolicy\n                \\sprefix-(?P<afi>\\S+)\n                \\s(?P<name>\\S+)\n                \\srule\\s(?P<sequence>\\d+)\n                \\sge\\s'(?P<ge>\\d+)'\n                $", re.VERBOSE), 'setval': "policy prefix-{{ 'list' if afi == 'ipv4' else 'list6' }} {{ name }} rule {{ sequence }} ge '{{ ge }}'", 'result': {"{{ 'ipv4' if afi == 'list' else 'ipv6' }}": {'afi': "{{ 'ipv4' if afi == 'list' else 'ipv6' }}", 'prefix_lists': {'{{ name }}': {'name': '{{ name }}', 'entries': {'{{ sequence }}': {'sequence': '{{ sequence }}', 'ge': '{{ ge }}'}}}}}}}, {'name': 'le', 'getval': re.compile("\n                ^set\n                \\spolicy\n                \\sprefix-(?P<afi>\\S+)\n                \\s(?P<name>\\S+)\n                \\srule\\s(?P<sequence>\\d+)\n                \\sle\\s'(?P<le>\\d+)'\n                $", re.VERBOSE), 'setval': "policy prefix-{{ 'list' if afi == 'ipv4' else 'list6' }} {{ name }} rule {{ sequence }} le '{{ le }}'", 'result': {"{{ 'ipv4' if afi == 'list' else 'ipv6' }}": {'afi': "{{ 'ipv4' if afi == 'list' else 'ipv6' }}", 'prefix_lists': {'{{ name }}': {'name': '{{ name }}', 'entries': {'{{ sequence }}': {'sequence': '{{ sequence }}', 'le': '{{ le }}'}}}}}}}, {'name': 'prefix', 'getval': re.compile("\n                ^set\n                \\spolicy\n                \\sprefix-(?P<afi>\\S+)\n                \\s(?P<name>\\S+)\n                \\srule\\s(?P<sequence>\\d+)\n                \\sprefix\\s'(?P<prefix>\\S+)'\n                $", re.VERBOSE), 'setval': "policy prefix-{{ 'list' if afi == 'ipv4' else 'list6' }} {{ name }} rule {{ sequence }} prefix '{{ prefix }}'", 'result': {"{{ 'ipv4' if afi == 'list' else 'ipv6' }}": {'afi': "{{ 'ipv4' if afi == 'list' else 'ipv6' }}", 'prefix_lists': {'{{ name }}': {'name': '{{ name }}', 'entries': {'{{ sequence }}': {'sequence': '{{ sequence }}', 'prefix': '{{ prefix }}'}}}}}}}]