import calendar
import collections.abc
import datetime
import warnings
from google.protobuf.internal import field_mask
def ToSeconds(self):
    """Converts a Duration to seconds."""
    return self.seconds