import os
from ..base import (
from ...external.due import BibTeX
from .base import (
class RemlfitInputSpec(AFNICommandInputSpec):
    in_files = InputMultiPath(File(exists=True), desc='Read time series dataset', argstr='-input "%s"', mandatory=True, copyfile=False, sep=' ')
    matrix = File(desc="the design matrix file, which should have been output from Deconvolve via the 'x1D' option", argstr='-matrix %s', mandatory=True)
    polort = traits.Int(desc="if no 'matrix' option is given, AND no 'matim' option, create a matrix with Legendre polynomial regressorsup to the specified order. The default value is 0, whichproduces a matrix with a single column of all ones", argstr='-polort %d', xor=['matrix'])
    matim = File(desc="read a standard file as the matrix. You can use only Col as a name in GLTs with these nonstandard matrix input methods, since the other names come from the 'matrix' file. These mutually exclusive options are ignored if 'matrix' is used.", argstr='-matim %s', xor=['matrix'])
    mask = File(desc='filename of 3D mask dataset; only data time series from within the mask will be analyzed; results for voxels outside the mask will be set to zero.', argstr='-mask %s', exists=True)
    automask = traits.Bool(usedefault=True, argstr='-automask', desc='build a mask automatically from input data (will be slow for long time series datasets)')
    STATmask = File(desc="filename of 3D mask dataset to be used for the purpose of reporting truncation-to float issues AND for computing the FDR curves. The actual results ARE not masked with this option (only with 'mask' or 'automask' options).", argstr='-STATmask %s', exists=True)
    addbase = InputMultiPath(File(exists=True, desc='file containing columns to add to regression matrix'), desc='file(s) to add baseline model columns to the matrix with this option. Each column in the specified file(s) will be appended to the matrix. File(s) must have at least as many rows as the matrix does.', copyfile=False, sep=' ', argstr='-addbase %s')
    slibase = InputMultiPath(File(exists=True, desc='file containing columns to add to regression matrix'), desc="similar to 'addbase' in concept, BUT each specified file must have an integer multiple of the number of slices in the input dataset(s); then, separate regression matrices are generated for each slice, with the first column of the file appended to the matrix for the first slice of the dataset, the second column of the file appended to the matrix for the first slice of the dataset, and so on. Intended to help model physiological noise in FMRI, or other effects you want to regress out that might change significantly in the inter-slice time intervals. This will slow the program down, and make it use a lot more memory (to hold all the matrix stuff).", argstr='-slibase %s')
    slibase_sm = InputMultiPath(File(exists=True, desc='file containing columns to add to regression matrix'), desc="similar to 'slibase', BUT each file much be in slice major order (i.e. all slice0 columns come first, then all slice1 columns, etc).", argstr='-slibase_sm %s')
    usetemp = traits.Bool(desc="write intermediate stuff to disk, to economize on RAM. Using this option might be necessary to run with 'slibase' and with 'Grid' values above the default, since the program has to store a large number of matrices for such a problem: two for every slice and for every (a,b) pair in the ARMA parameter grid. Temporary files are written to the directory given in environment variable TMPDIR, or in /tmp, or in ./ (preference is in that order)", argstr='-usetemp')
    nodmbase = traits.Bool(desc="by default, baseline columns added to the matrix via 'addbase' or 'slibase' or 'dsort' will each have their mean removed (as is done in Deconvolve); this option turns this centering off", argstr='-nodmbase', requires=['addbase', 'dsort'])
    dsort = File(desc='4D dataset to be used as voxelwise baseline regressor', exists=True, copyfile=False, argstr='-dsort %s')
    dsort_nods = traits.Bool(desc="if 'dsort' option is used, this command will output additional results files excluding the 'dsort' file", argstr='-dsort_nods', requires=['dsort'])
    fout = traits.Bool(desc='output F-statistic for each stimulus', argstr='-fout')
    rout = traits.Bool(desc='output the R^2 statistic for each stimulus', argstr='-rout')
    tout = traits.Bool(desc="output the T-statistic for each stimulus; if you use 'out_file' and do not give any of 'fout', 'tout',or 'rout', then the program assumes 'fout' is activated.", argstr='-tout')
    nofdr = traits.Bool(desc="do NOT add FDR curve data to bucket datasets; FDR curves can take a long time if 'tout' is used", argstr='-noFDR')
    nobout = traits.Bool(desc="do NOT add baseline (null hypothesis) regressor betas to the 'rbeta_file' and/or 'obeta_file' output datasets.", argstr='-nobout')
    gltsym = traits.List(traits.Either(traits.Tuple(File(exists=True), Str()), traits.Tuple(Str(), Str())), desc="read a symbolic GLT from input file and associate it with a label. As in Deconvolve, you can also use the 'SYM:' method to provide the definition of the GLT directly as a string (e.g., with 'SYM: +Label1 -Label2'). Unlike Deconvolve, you MUST specify 'SYM: ' if providing the GLT directly as a string instead of from a file", argstr='-gltsym "%s" %s...')
    out_file = File(desc="output dataset for beta + statistics from the REML estimation; also contains the results of any GLT analysis requested in the Deconvolve setup, similar to the 'bucket' output from Deconvolve. This dataset does NOT get the betas (or statistics) of those regressors marked as 'baseline' in the matrix file.", argstr='-Rbuck %s')
    var_file = File(desc='output dataset for REML variance parameters', argstr='-Rvar %s')
    rbeta_file = File(desc="output dataset for beta weights from the REML estimation, similar to the 'cbucket' output from Deconvolve. This dataset will contain all the beta weights, for baseline and stimulus regressors alike, unless the '-nobout' option is given -- in that case, this dataset will only get the betas for the stimulus regressors.", argstr='-Rbeta %s')
    glt_file = File(desc="output dataset for beta + statistics from the REML estimation, but ONLY for the GLTs added on the REMLfit command line itself via 'gltsym'; GLTs from Deconvolve's command line will NOT be included.", argstr='-Rglt %s')
    fitts_file = File(desc='output dataset for REML fitted model', argstr='-Rfitts %s')
    errts_file = File(desc='output dataset for REML residuals = data - fitted model', argstr='-Rerrts %s')
    wherr_file = File(desc='dataset for REML residual, whitened using the estimated ARMA(1,1) correlation matrix of the noise', argstr='-Rwherr %s')
    quiet = traits.Bool(desc='turn off most progress messages', argstr='-quiet')
    verb = traits.Bool(desc='turns on more progress messages, including memory usage progress reports at various stages', argstr='-verb')
    goforit = traits.Bool(desc='With potential issues flagged in the design matrix, an attempt will nevertheless be made to fit the model', argstr='-GOFORIT')
    ovar = File(desc='dataset for OLSQ st.dev. parameter (kind of boring)', argstr='-Ovar %s')
    obeta = File(desc='dataset for beta weights from the OLSQ estimation', argstr='-Obeta %s')
    obuck = File(desc='dataset for beta + statistics from the OLSQ estimation', argstr='-Obuck %s')
    oglt = File(desc="dataset for beta + statistics from 'gltsym' options", argstr='-Oglt %s')
    ofitts = File(desc='dataset for OLSQ fitted model', argstr='-Ofitts %s')
    oerrts = File(desc='dataset for OLSQ residuals (data - fitted model)', argstr='-Oerrts %s')