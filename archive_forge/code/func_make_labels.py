import pytest
from string import ascii_letters
from random import randint
import gc
import sys
def make_labels():
    o = []
    for text in labels:
        o.append(cls(text=text))
    if tick == 'tick':
        Clock.tick()